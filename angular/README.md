Angular presets demo

Instructions:

- From this workspace run:

```
cd angular
npm install
npx ng serve
```

The app will call the presets API at `http://localhost:3000/api/presets`.

Edit `src/environments/environment.ts` to change the API base if needed.
